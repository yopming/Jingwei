var fs = require('fs');
var path = require('path');
var dataConfig = require('../configs/data.js');


/*
 * Touch under the specific directory
 * @param directory name
 * @param new page name
 */
function pageTouch(dir, name) {
}
